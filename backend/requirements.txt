# LiveKit
livekit==1.0.23
livekit_api==1.1.0
livekit_blingfire==1.1.0
livekit_plugins_noise_cancellation==0.2.5
livekit-agents==1.3.12
livekit-plugins-google==1.3.12

# LLM & tools
mem0ai
duckduckgo-search
langchain_community==0.4.2
Requests==2.34.2
python-dotenv==1.2.3
pydantic==2.13.5

# FastAPI Server (untuk voice verification endpoint)
fastapi==0.141.1
uvicorn[standard]
python-multipart
cryptography==50.0.1

# Numeric
numpy
scipy

# Audio
librosa==0.11.0
soundfile==0.13.1
sounddevice==0.5.3

# ML utils & Computer Vision
scikit-learn==1.3.2
matplotlib
insightface==1.0.1
opencv_python==4.11.0.86

# Testing (voiceverification/tests/) — run with: pytest
pytest==9.1.1

# Speaker recognition
speechbrain==0.5.16
huggingface_hub==0.16.4
# torchaudio's default backend needs torchcodec (which itself needs the
# system `ffmpeg` binary + shared libs — `brew install ffmpeg` on macOS,
# already required by utils/ffmpeg.py for webm->wav conversion) to load
# audio at all; without it, SpeakerVerifier.extract_embedding() fails.
torchcodec

# Anti-spoofing (pretrained deepfake/replay detector, core/asvspoof.py)
# transformers MUST stay <4.31 here: newer releases require a huggingface_hub
# major version that removed the use_auth_token kwarg speechbrain 0.5.16
# still calls — upgrading transformers alone silently breaks speaker
# verification. Bump both together only after confirming SpeakerVerifier
# still loads.
transformers==4.30.2

webrtcvad==2.0.10

supabase==2.31.0

# Torch (CPU by default via PyPI's default index).
# torchaudio's last published release is 2.11.0 — it has not kept pace with
# torch's version numbers, so torch==2.13.0 + torchaudio==2.11.0 is the
# correct/best available pairing, not a mismatch. For a CUDA deployment,
# install these two from https://pytorch.org/get-started/locally/ instead
# (matching your CUDA version) before `pip install -r requirements.txt`,
# then drop this file's copies so pip doesn't override your GPU build.
torch==2.13.0
torchaudio==2.11.0